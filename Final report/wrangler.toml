name = "food-order-at-table-system"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Serve static files (HTML, images, JS, etc.)
[assets]
directory = "./"

[vars]
# Non-sensitive variables go here

# API key is added as a secret via Cloudflare Dashboard
# Or using: wrangler secret put GEMINI_API_KEY
